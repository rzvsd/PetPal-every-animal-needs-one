# Archive

This folder keeps old or retired project targets so they do not sit next to the active app.

## old-ui/PetPalMobileBuild

Historical Expo / React Native prototype. It is archived because the active Android app currently installed on the phone is the Capacitor app under `frontend`.

Do not use this folder for fresh UI/UX work unless the project intentionally returns to the Expo / React Native target.
