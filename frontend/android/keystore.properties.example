# Copy this file to frontend/android/keystore.properties or set the matching
# PETPAL_RELEASE_* environment variables in CI.
#
# Never commit the real keystore or passwords.
storeFile=../release/petpal-upload-key.jks
storePassword=change-me
keyAlias=petpal-upload
keyPassword=change-me
